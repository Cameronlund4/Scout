<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".layout.EventListFragment">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/eventListRecycler"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <RelativeLayout
        android:id="@+id/eventListLoading"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:visibility="gone">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:textSize="30sp"
            android:text="@string/loading"
            android:id="@+id/eventListLoadingSuper"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:textSize="15sp"
            android:singleLine="true"
            android:text="Detailed text that ends in..."
            android:ellipsize="end"
            android:id="@+id/eventListLoadingSub"
            android:layout_below="@id/eventListLoadingSuper"/>
    </RelativeLayout>
</RelativeLayout>
